## Functions in Golang


* Functions are treated as values in golang and are assignable.
* Function Types can be defined 
* Functions are a bridge to interfaces
* Functions inside a function are called as "Closuers", meaning anonymous functions can modify the outer varaible's data.
* Functions can be used as parameters much like comparator function used by std::vector while sorting
* We can return functions from fucnctions jsut like we return normal values.

## The Defer keyword

* Don't forget to free up the resources. defer helps us d othat at the end of function
* For multiple it works like stack


